apiVersion: apps/v1
kind: Deployment
metadata:
  name: podfleet-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: podfleet-operator
  template:
    metadata:
      labels:
        name: podfleet-operator
    spec:
      serviceAccountName: podfleet-operator
      containers:
        - name: podfleet-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - podfleet-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "podfleet-operator"
